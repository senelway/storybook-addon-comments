// addons, panels and events get unique names using a prefix
export const ADDON_ID = 'storybooks/storybook-addon-comments';
export const EVENT_ID = `${ADDON_ID}/event`;

export const RegexpReplace = / /g;
